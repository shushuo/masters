{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions, the shell capability to spawn the getmastersd sidecar, and the updater + process (relaunch) capabilities for in-app auto-update.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [{ "name": "binaries/getmastersd", "sidecar": true }]
    },
    "updater:default",
    "process:allow-restart"
  ]
}
